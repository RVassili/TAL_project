#ifndef CUSTOM_STRING_H_INCLUDED
#define CUSTOM_STRING_H_INCLUDED

#include <string.h>
#include <stdlib.h>
#include <stdio.h>

/*
struct TStructReturnBalise
{
    char *pStartBalise,
         *pEndBalise;

    TStructReturnBalise() : pStartBalise(NULL), pEndBalise(NULL)
    {

    }
};
*/
/* fonctions communes au parseur de xml et au parseur de fichers du corpus */
//TStructReturnBalise FindBalise(char *pFile, char cDelimiter, char cDelimiterClose);

/* fonctions du parseur de xml */

/* fonction du parseur de fichiers du corpus */


#endif // CUSTOM_STRING_H_INCLUDED
